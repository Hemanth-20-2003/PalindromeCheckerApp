# PalindromeCheckerApp

## UC8: Linked List-Based Palindrome Checker
This use case checks whether a string is a palindrome using a singly linked list. The string is first converted into a linked list structure, after which the middle of the list is found using the fast and slow pointer technique. The second half of the list is reversed and then compared with the first half to determine whether the sequence of characters forms a palindrome.