# PalindromeCheckerApp

## UC12: Strategy Pattern for Palindrome Algorithms
This use case implements palindrome checking using the Strategy Design Pattern, allowing the algorithm to be selected dynamically at runtime. A PalindromeStrategy interface defines the common behavior, while different implementations such as StackStrategy and DequeStrategy provide specific palindrome validation approaches. This demonstrates polymorphism and flexible algorithm selection while keeping the application modular and extensible.