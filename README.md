# PalindromeCheckerApp

## UC2: Print a Hardcoded Palindrome Result
This use case demonstrates a basic palindrome check using a hardcoded string. The program evaluates whether the predefined word reads the same forward and backward using a conditional statement and prints the result on the console. It introduces core Java concepts such as classes, the main method, string literals, and basic control flow.
