# PalindromeCheckerApp

## UC1: Application Entry & Welcome Message
This use case initializes the Palindrome Checker application and displays a welcome message along with application details. The Java Virtual Machine invokes the `main()` method as the entry point, and the program prints the application name and version using console output. It defines the initial flow of the application before proceeding to further palindrome operations.
