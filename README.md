# PalindromeCheckerApp

## UC7: Deque Based Optimized Palindrome Checker
This use case validates a palindrome using a Deque (Double Ended Queue). Characters from the input string are inserted into the deque and compared by removing elements from both ends using removeFirst() and removeLast(). This approach avoids reversing the string and efficiently checks the palindrome using bidirectional traversal.