# PalindromeCheckerApp

## UC9: Recursive Palindrome Checker
This use case checks whether a string is a palindrome using recursion. The algorithm recursively compares characters from the beginning and end of the string, moving towards the center with each call. A base condition stops the recursion when all characters are validated, demonstrating how the call stack manages recursive method execution.