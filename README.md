# PalindromeCheckerApp

## UC11: Object-Oriented Palindrome Service
This use case implements palindrome checking using object-oriented design principles. The palindrome logic is encapsulated inside a dedicated PalindromeChecker class that exposes a checkPalindrome() method. This follows the principles of encapsulation and single responsibility, making the code modular, reusable, and easier to maintain.