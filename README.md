# PalindromeCheckerApp
## UC3: Palindrome Check Using String Reverse
This use case checks whether a string is a palindrome by reversing the string using a loop. The reversed string is constructed character by character and then compared with the original string using the `equals()` method. It demonstrates concepts such as loops, string immutability, and string concatenation.
